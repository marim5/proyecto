def usuario(nombre):
    print(nombre)


def principal(parametro):
    print ("Mujeres en ingenieria")

def recorrido(parametro):
    print("akjdfsh")

def testimonios(parametro):
    print("lkjhdf")

def historias(parametro):
    print("aksjdh")

def q_a(parametro):
    print("Esta es una sección para que hagas todas las preguntas que tengas. No solo las responderemos sino que tendrás acceso a todas las preguntas que han hecho previamente.")
    #hacer un diccionario en donde se almacene pregunta y respuesta o pueden iniciar sesión acá, llevar un contador de cuántas preguntas se hacen con iniciar sesión o
